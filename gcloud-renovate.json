{
    "regexManagers": [
        {
            "fileMatch": [
                "{{arg0}}"
            ],
            "matchStrings": [
                " +['\"]?version['\"]? *: +['\"]?(?<currentValue>[^'\" \\n]+)['\"]? +# renovate: depName=gcp/cli/gcloud[ \\n]",
                " +['\"]?name['\"]? *: +['\"]?gcp/cli/gcloud@(?<currentValue>[^'\" \\n]+)['\"]?"
            ],
            "extractVersionTemplate": "^(?<version>.*)$",
            "datasourceTemplate": "github-tags",
            "depNameTemplate": "twistedpair/google-cloud-sdk"
        }
    ]
}
